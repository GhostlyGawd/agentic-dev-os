# Traceability

`traceability.json` is canonical. A record is complete only when every reference resolves and the requirement appears in its PRD. `python scripts/ados.py validate` enforces this contract.

Statuses: `Planned`, `In Progress`, `Blocked`, `Complete`, `Archived`.
