# Least Privilege

Grant only the paths, commands, network destinations, data, and duration needed by the active ticket. R0 is read-only; R1 is reversible and scoped; R2 requires prior approval; R3 requires approval at action time. Never persist credentials in events. Approval does not expand file scope. Record external actions and reversals. Production and person-directed actions remain outside this starter's autonomous execution boundary.
