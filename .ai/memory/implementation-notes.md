# Implementation Notes

- Keep product discovery Markdown readable and validate only durable contract fields.
- Derive requirements and CSV exports from PRDs, specs, and canonical trace JSON.
- Treat user feedback as external evidence, not an automated metric substitute.
- Record repeated failures under `docs/findings/` and promote them into permanent guards.
